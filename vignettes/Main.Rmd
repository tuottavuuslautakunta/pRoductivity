---
title: "Päätoimialat"
author: "Janne Huovari"
date: "`r Sys.Date()`"
output: rmarkdown::html_document

---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  fig.path = "fig_intro/",
  collapse = TRUE,
  comment = "#>"
)

devtools::load_all()

library(tidyverse)
library(glue)
library(ggptt)

set_ptt()

# Data

data("dat_stan_main", "dat_nama_main")

countries <- c(Suomi = "FI", Ruotsi = "SE", Saksa = "DE", Britannia = "UK", 
                        Ranska = "FR", Tanska = "DK", Italia = "IT", Alankomaat = "NL", 
                        Norja = "NO", EU15 = "EU15", USA = "US")



start_time <- 2000
base_year <- 2008

high_country <- "Suomi"

dat_main_groups <- data_main_groups %>% 
  filter(time >= start_time)



```


```{r lp}

dat_main_groups %>% 
  ggplot(aes(time, lp_ind, colour = geo)) +
  facet_wrap(~ nace0) +
  geom_line()

```

```{r lp_private}

dat_main_groups %>% 
  filter(nace0 == "private") %>% 
  prod_ind_plot()
  
```

```{r lp_manu}

dat_main_groups %>% 
  filter(nace0 == "manu") %>% 
  prod_ind_plot()
  
```


```{r lp_service}

dat_main_groups %>% 
  filter(nace0 == "service") %>% 
  prod_ind_plot() 

```
