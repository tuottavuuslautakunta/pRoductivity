---
title: "Päätoimialat"
author: "Janne Huovari"
date: "`r Sys.Date()`"
output: rmarkdown::html_document

---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  fig.path = "fig_intro/",
  fig.width = 9,
  fig.height = 4,
  collapse = TRUE,
  comment = "#>"
)

devtools::load_all()

library(tidyverse)
library(glue)
library(ggptt)

# set_ptt()

tula_pal <- function(n) c("#6495ED", "#6E8B3D", "#EE7600", "#8B3E2F", "#8968CD", "#f03b20", "grey75")[1:n]

set_gg(theme_ptt(), tula_pal)

# Data

data("data_main", "data_main_groups")


high_country <- "Suomi"
high_countries <- c("Ruotsi", "Euroalue-12", "Saksa", "Yhdysvallat", "Tanska")


```


```{r lp}

data_main_groups %>% 
  ggplot(aes(time, lp_ind, colour = geo)) +
  facet_wrap(~ nace0) +
  geom_line()

```

```{r lp_private}

data_main_groups %>% 
  filter(nace0 == "private") %>% 
  prod_ind_plot()
  
```

```{r lp_private_high}

data_main_groups %>% 
  filter(nace0 == "private") %>% 
  prod_ind_plot_high()

  
```

```{r lp_private_ex26_high}

data_main_groups %>% 
  filter(nace0 == "private_ex26") %>% 
  prod_ind_plot_high()

  
```

```{r lp_manu}

data_main_groups %>% 
  filter(nace0 == "manu") %>% 
  prod_ind_plot()
  
```

```{r lp_manu_high}

data_main_groups %>% 
  filter(nace0 == "manu") %>% 
  prod_ind_plot_high()

  
```

```{r lp_manu_ex_high}

data_main_groups %>% 
  filter(nace0 == "manu_ex26") %>% 
  prod_ind_plot_high()

  
```

```{r lp_service}

data_main_groups %>% 
  filter(nace0 == "service") %>% 
  prod_ind_plot() 

```
```{r lp_serv_high}

data_main_groups %>% 
  filter(nace0 == "service") %>% 
  prod_ind_plot_high()

  
```
